# Introduction

Machine learning samples using PyTorch, TensorFlow, JAX, SciPy and scikit-learn

## Create environment

```shell
# You should have pip and Python3
pip install virtualenv
virtualenv .venv
source .venv/bin/activate
pip install -r requirements.txt
```
